import Button from '../button/Button';
import Input from '../input/Input';
import { CreateForm } from "./Form.styles";

const Form = () => {
    return (
        <CreateForm>
          <Input />
          <Button />
        </CreateForm>
    )
  }
  
export default Form;